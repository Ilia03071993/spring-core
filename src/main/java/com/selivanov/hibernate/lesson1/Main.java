package com.selivanov.hibernate.lesson1;

import com.selivanov.hibernate.lesson1.config.AppConfig;
//import com.selivanov.hibernate.lesson1.entity.Product;
//import com.selivanov.hibernate.lesson1.repository.ProductRepository;
import org.springframework.context.annotation.AnnotationConfigApplicationContext;

import java.util.List;

public class Main {
    public static void main(String[] args) {
    //    AnnotationConfigApplicationContext context =
       //         new AnnotationConfigApplicationContext(AppConfig.class);
     //   ProductRepository repository = context.getBean(ProductRepository.class);

//        Product product = new Product();
//        product.setName("Milk");
//        product.setPrice(100);
//        product.setExpirationDate(LocalDate.of(2023, 11, 3));
//
        //Product product = new Product(,"Bread",50, LocalDate.of(2023,10,26));
      //  repository.removeProduct(2L);

     //   List<Product> allProducts = repository.findAllProducts();
     //   System.out.println(allProducts);

     //   context.close();
    }
}
